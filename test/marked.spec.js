import test from 'ava';
import 'babel-core/register';
import Marked from '../lib/marked';

test('#constructor', t => {
  const marked = new Marked();
});

test('#parse', t => {
  const marked = new Marked();
});
